{
  "name": "chatapp-whatsapp-clone",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && node server.js",
    "build": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
